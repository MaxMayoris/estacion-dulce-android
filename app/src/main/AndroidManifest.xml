<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo_ed_circle_144x144"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_ed_circle_144x144"
        android:supportsRtl="true"
        android:theme="@style/Theme.EstacionDulceApp"
        tools:targetApi="31">

        <!-- Declare LoginActivity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <!-- Set LoginActivity as the Launcher Activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.ProductEditActivity" />
        <activity android:name=".activities.RecipeEditActivity" />
        <activity android:name=".activities.PersonEditActivity" />
        <activity android:name=".activities.MovementEditActivity" />
    </application>

</manifest>
