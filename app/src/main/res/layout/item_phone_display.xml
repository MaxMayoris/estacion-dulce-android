<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/phone_item_background"
    android:gravity="center_vertical">

    <!-- Phone Icon -->
    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="12dp"
        android:src="@drawable/ic_phone"
        app:tint="@android:color/holo_blue_dark" />

    <!-- Phone Number -->
    <TextView
        android:id="@+id/phoneNumberText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="264 1234567"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:textStyle="bold"
        android:clickable="true"
        android:focusable="true"
        android:background="?attr/selectableItemBackground"
        android:padding="8dp" />

    <!-- Copy Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/copyPhoneButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="Copiar número de teléfono"
        app:backgroundTint="@android:color/holo_blue_dark"
        app:cornerRadius="20dp"
        app:icon="@drawable/ic_copy"
        app:iconGravity="textStart"
        app:iconPadding="0dp"
        app:iconSize="20dp"
        app:iconTint="@android:color/white"
        style="@style/Widget.Material3.Button.IconButton" />

    <!-- Delete Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/deletePhoneButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:contentDescription="Eliminar teléfono"
        app:backgroundTint="@android:color/holo_red_dark"
        app:cornerRadius="20dp"
        app:icon="@drawable/ic_delete"
        app:iconGravity="textStart"
        app:iconPadding="0dp"
        app:iconSize="20dp"
        app:iconTint="@android:color/white"
        style="@style/Widget.Material3.Button.IconButton" />

</LinearLayout>
